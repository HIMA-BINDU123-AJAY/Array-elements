This program calculates the sum of elements in an array by iterating through each value and adding them.

Features:
.Takes array input
.Uses loop to calculate sum
.Displays total sum

Example:
Input: [1, 2, 3, 4]
Output: 10

Technologies Used:
.Python
